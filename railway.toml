[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -w 1 --bind 0.0.0.0:$PORT main:app"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
healthcheckPath = "/"
healthcheckTimeout = 30
